function añadir(descripcion) {
  alert("No es posible registrar una tarea pendiente sin descripción.");

  /*for(i=0; i<fields.length; i++){
    document.getElementById(fields[i]).maxLength = 50;
  }
  var maximo = 50;
  if (descripcion > maximo) {
    return descripcion;
  }
  else{
    alert("Logitud maxima sobrepasa !!!");
  }*/
  return descripcion;
}

export default añadir;
